import React from 'react';
import PropTypes from 'prop-types';

const Hero = props => {
  return (
    <div>
      Hero
    </div>
  );
};

Hero.propTypes = {
  posts: PropTypes.array
};

export default Hero;